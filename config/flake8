[flake8]
# We usually have aligned assignments and arrays, so we ignore these errors
ignore =
    E221,  # multiple spaces before operator
    E241,  # multiple spaces after ','
    E201,  # whitespace after '('
    E202,  # whitespace before ')'

# vim: ft=config
