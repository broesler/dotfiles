	pager = ,V
s,v
    echo "$tmuxswp",v
    echo "$tmuxswp",V
test,v
  echo $pat,V
 ,v
o,v
-,v
  echo "$test",V
matlab,v
$@,v
  echo $@,V
$msid:$mwid.$mpid,v
        tmux select-pane -t "$msid:$mwid.$mpid" ,V
        tmux send-keys -t "$msid:$mwid.$mpid" "$@" C-m,V
[A]pplications/MATLAB,v
,V
    # list pane tty, window id, pane id:    #   /dev/ttys001 $1 @5 %10 /dev/ttys002 $1 @5 %12    lsp=$(tmux list-panes -F "#{pane_tty} #{session_id} #{window_id} #{pane_id}")    # read ttys, windows, panes into array    read -r -a panetty  <<< $(echo "$lsp" | \grep -o "tty.[0-9]\{3\}")    read -r -a sessid   <<< $(echo "$lsp" | \grep -o "\$[0-9]\+")    read -r -a windowid <<< $(echo "$lsp" | \grep -o "@[0-9]\+")    read -r -a paneid   <<< $(echo "$lsp" | \grep -o "%[0-9]\+")    # NOTE: to do the same without Perl regex (-P), need to do 2 greps to    #   ensure you are getting what is immediately following tty.:    # read -r -a panetty  <<< $(echo "$lsp" | \grep -o "tty.[0-9]\{3\}" | \grep -o "[0-9]\{3\}")    # Build regex pattern to search all tty's    pat="("    i=1    for pane in ${panetty[@]}; do      pat+=$pane      if [ "$i" -ne "${#panetty[@]}" ]; then        pat+='|'      fi      let i+=1    done     pat+=')'    # grep for matlab running in one of the pane tty's    test=$(\ps | \egrep "$pat" | \grep  "[A]pplications/MATLAB")    # extract tty of pane running matlab    mtty=$(echo "$test" | \grep -o "tty.[0-9]\{3\}")    # find array index matching mtty    i=0    for k in ${panetty[@]}; do        if [ "$k" == "$mtty" ]; then          mind=$i        fi        let i+=1    done    # extract window and pane id's (window is current window)    msid=${sessid[$mind]}    mwid=${windowid[$mind]}    mpid=${paneid[$mind]},V
" nnoremap <Leader>n :call system('ts "dbstep"'<bar>redraw!<CR>" nnoremap <Leader>n :call Dbstep()<bar>redraw!<CR>,V
<bar>redraw!,v
  " call command (ts already has carriage return built-in,V
r,v
:,v
<bar>redraw!<CR>,v
\,v
$mtty,v
 $,v
i,v
, which ,v
    # use bottom-left as default,v
        # if no arguments given, open MATLAB in bottom-left tmux pane        tmux send-keys -t bottom-left 'matlab -nosplash -nodesktop' C-m,V
        # move to pane with instance of matlab        echo "Matlab is running in $msid:$mwid.$mpid, which is $mtty."        tmux select-pane -t "$msid:$mwid.$mpid" ,V
      # if matlab is not running, choose bottom-left pane as default      if [ ! -n "$test" ]; then      else ,V
      fi,V
    if (($#)); then,V
ttys,v
P,v
),v
(?<=,v
[t]ty.,v
window,v
    read -r -a windowid <<< $(echo "$lsp" | \grep -o "@[0-9]\+"),V
    echo "$pat",V
(?<=%),v
(?<=@),v
$mwid.,v
    # echo "Matlab is running in $mwid.$mpid, which is ttys$mtty.",V
 bottom-left,v
"$mwid.$mpid" ,v
"$mwid.$mpid",v
        # echo "Matlab is running in $mwid.$mpid, which is ttys$mtty.",V
on ,v
act,v
assume it is in bottom-left pane,v
    echo "$mtty",V
    # read -r -a panetty  <<< $(echo "$lsp" | \grep -oP "(?<=tty.)[0-9]{3}"),V
    read -r -a panetty  <<< $(echo "$lsp" | \grep -oP "(?<=tty.)[0-9]{3}"),V
mpane,v
    test=$(ps | egrep "$pat" | grep  "[A]pplications/MATLAB"),V
],v
/,v
    echo "$test",V
    echo "${panetty[@]}"    echo "${windowid[@]}"    echo "${paneid[@]}",V
windowid,v
    echo "${windowid[@]}",V
panetty,v
    echo "${panetty[@]}",V
{3,v
tty.,v
{3},v
.,v
y,v
t,v
    read -r -a panetty <<< $(echo "$lsp" | \grep -oP "(?<=tty.)[0-9]{3}"),V
    lsp=$(tmux list-panes -F "#{pane_tty} #{window_id} #{pane_id}"),V
    read -r -a panetty <<< ,V
$ttys,v
    ttys=,v
    # /dev/ttys001 ,V
    # Get all tty's of panes in this window into panetty array,V
  ,v
/3)),v
$((,v
    echo $temp2,V
temp2,v
temp1,v
    echo $temp1,V
tmux list-panes -F "#{pane_tty} #{window_id} #{pane_id}",v
    IFS=' ' ,v
      ,v
 \,v
    #,V
    # See if matlab is running in this tmux window,V
    #---------------------------------------------------------------------------,V
 (if at all),v
        fg /Applications/MATLAB_R2015b.app/bin/matlab ,v
        fg /Applications/MATLAB_R2015b.app/bin/matlab ,V
&& ,v
&,v
